---
draft: false
date: 2026-03-08
categories:
  - Releases
---

# CHILI GraFx Environment API Update

![rn_icon](/assets/icon-CHILI-GraFx.svg){.rn_icon}

## New & Improved

### Subject types for media now visible

You can now retrieve subject types attached to media and external media — something that wasn't possible before. Use `GET /vision?expand=allSubjectTypes` to get the full overview in a single call.

### Font lists show text when a preview is unavailable

Font lists in your integrations now display fallback text when a font preview can't be shown, instead of leaving an empty spot. Note: some fonts may still not preview correctly — a full fix is planned for a future release.

### Update a single subject type without affecting others

You can now update individual subject types directly, on both GraFx and external environments, using `PATCH /vision?subjectType={subjectId}`.

## More info

Read more on the [CHILI GraFx API documentation](/GraFx-Developers/environment-api/reference/)
