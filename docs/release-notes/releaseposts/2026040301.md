---
draft: false
date: 2026-04-03
categories:
  - Releases
---

# GraFx Studio: Components, image crop, and improvements

![GraFx Studio icon](/assets/icon-GraFx-Studio.svg){.rn_icon}

## ✨ New & Improved

### Components — build once, reuse everywhere

Components are reusable design elements that can be placed inside templates. Design a pricing block, a product ad, or any branded element once — then place it across as many templates as you need. When you update a component, every template that uses it reflects the change automatically.

**What you can do with components:**

- **Place a component as a frame** — open the Resources panel in the template workspace, browse or search for a component, and click to place it on the canvas
- **Use multiple instances** — place the same component multiple times on a single page, each with its own position, size, and data
- **Map variables per instance** — connect each component instance to its own set of template variables, so a coupon sheet with six coupons can show six different prices from the same pricing component
- **Choose a resize mode** — control how the component fills its frame: Scale (picks the best-matching layout by aspect ratio), Resize (applies internal anchoring and copyfitting rules), or Scale and resize (combines both)

**For template designers:**

Components have their own workspace in GraFx Studio, alongside Templates and Collections. Each component can have multiple layouts, its own Brand Kit, variables, and actions. The component workspace mirrors the template designer workspace, with a focused set of features for reusable design elements.

> Variable mapping is per instance. A template with three instances of the same component can map each to a completely different set of variables — giving each instance its own independent data.

See [Components](/GraFx-Studio/concepts/components/) for a full introduction, including use cases and how components relate to templates.

**Guides:**

- [Build a component](/GraFx-Studio/guides/build-component/)
- [Use components in a template](/GraFx-Studio/guides/use-components/)

### Image crop constraint for end users

Template designers can now control whether end users are allowed to crop images within a frame. This is a per-frame setting — disabled by default.

When crop is enabled for a frame, end users see a crop icon alongside the existing rotation icon. Activating crop mode lets the user drag the image within the frame to adjust what's visible. The frame size and position stay fixed — only the image's position inside the frame changes.

**A note on fit modes:**

- For frames using Fit, Fill, or Smart crop: each image keeps its own individual crop when adjusted by the end user.
- For frames using Manual crop: there is no per-asset crop. End users adjust the shared crop area of the frame, which applies to all images in it. If you want end users to crop images individually per asset, do not use Manual crop fit mode on that frame.

### Connector shared header value

Connector developers can now set a header value that applies across all current and future instances of a connector sharing the same remote connector ID. This makes it easier to pass shared configuration — such as an API key or session token — once, without needing to set it per connector instance.

### Font size constraint enabled by default

When text editing constraints are turned on for a text frame, the font size constraint is now enabled by default. Previously it was off, which could lead to unexpected results when the intent was to restrict all text editing properties.

## 🐛 Fixed

- **Templates with components now generate digital output correctly** — MP4, GIF, PNG, and JPG output previously failed with an internal error when a template contained components. This is resolved.
- **Missing font no longer crashes output** — if a font referenced in a template is not available, output generation now continues with a fallback rather than failing entirely.
- **Images can now be assigned to frames with Smart crop** — a regression had prevented new images from being assigned to frames that had Smart crop mode active. This is fixed.
- **Image crop in Manual crop mode now saves correctly** — crop settings applied in project (Run) mode were not being saved for images using Manual crop fit mode. This is now resolved.
- **Datasource modal error state** — the modal now correctly shows an empty or error state rather than displaying incorrectly when the datasource returns no data.
- **Text constraints rotation label** — a typo in the rotation info message inside the text constraints settings has been corrected.
- **Connector token refresh for auth type "none"** — when a connector is configured with authentication type "none", the token refresh flow now correctly handles this case. Previously, the missing authorization header caused the system to trigger the wrong refresh path, which could result in unexpected connector behaviour.
- **`getPageSnapshot` now returns meaningful errors** — calling `getPageSnapshot` with an invalid page ID previously returned an opaque, unhelpful error response. It now returns a clear, actionable error message, making it easier to diagnose integration issues. Note: page IDs changed from integers to GUIDs in a previous release — any integration hardcoding a numeric page ID will need to be updated.
